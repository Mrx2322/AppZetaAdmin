<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="12dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvNombreProducto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#202020"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="@string/admin_order_product_name_preview" />

        <TextView
            android:id="@+id/tvCantidadProducto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#888888"
            android:textSize="13sp"
            tools:text="@string/admin_order_product_quantity_preview" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:background="@drawable/bg_controles_cantidad"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btnRestar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:clickable="true"
            android:contentDescription="@string/admin_order_decrease_quantity"
            android:focusable="true"
            android:gravity="center"
            android:text="@string/admin_order_minus_symbol"
            android:textColor="#FF9500"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvCantidad"
            android:layout_width="35dp"
            android:layout_height="48dp"
            android:gravity="center"
            android:textColor="#202020"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="@string/admin_order_quantity_preview" />

        <TextView
            android:id="@+id/btnSumar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:clickable="true"
            android:contentDescription="@string/admin_order_increase_quantity"
            android:focusable="true"
            android:gravity="center"
            android:text="@string/admin_order_plus_symbol"
            android:textColor="#FF9500"
            android:textSize="22sp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>
